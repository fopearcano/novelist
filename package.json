{
  "name": "novelist",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 server.py",
    "start:local": "NOVELIST_HOST=127.0.0.1 python3 server.py",
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py'"
  }
}
